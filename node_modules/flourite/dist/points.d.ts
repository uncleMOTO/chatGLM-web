import type { LanguagePattern } from "./types";
/**
 * Get points from a language using regular expressions.
 * @param {String} lineOfCode
 * @param {LanguagePattern[]} checkers
 * @returns {Number}
 */
export declare function getPoints(lineOfCode: string, checkers: LanguagePattern[]): number;
/**
 * Checks if a given string is near top of the code or not.
 * @param {Number} index
 * @param {String[]} linesOfCode
 * @returns {Boolean}
 */
export declare function nearTop(index: number, linesOfCode: string[]): boolean;
